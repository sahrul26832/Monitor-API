{
  "name": "monitor-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --d1 DB",
    "deploy": "wrangler pages deploy public",
    "db:init": "wrangler d1 execute monitor-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute monitor-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "wrangler": "^3.99.0"
  }
}
